const defaultSettings = {
  position: 'bottom-left',
  hideTimeAgo: false,
  truncateProductName: true,
  displayDuration: 5,
  firstDelay: 5,
  popsInterval: 10,
  maxPopsDisplay: 10,
  includedUrls: '',
  excludedUrls: '',
  allowShow: 'all'
};

export default defaultSettings;
